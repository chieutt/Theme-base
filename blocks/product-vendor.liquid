{% doc %}
  Renders the product vendor.
{% enddoc %}
{% assign product = closest.product | default: product %}
{% if product.vendor != blank %}<p class="product-vendor" {{ block.shopify_attributes }}>{{ product.vendor | escape }}</p>{% endif %}
{% schema %}
{
  "name": "Product vendor",
  "settings": []
}
{% endschema %}

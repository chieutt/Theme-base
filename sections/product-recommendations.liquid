<div class="product-recommendations" data-recommendation-source data-section-id="{{ section.id }}">
  {% if recommendations.performed and recommendations.products_count > 0 %}
    {% if section.settings.heading != blank %}
      <{{ section.settings.html_tag | default: 'h2' }} class="product-recommendations__heading {% render 'typography-class', preset: section.settings.heading_size, fallback: 'h3' %}">{{ section.settings.heading | strip_html | escape }}</{{ section.settings.html_tag | default: 'h2' }}>
    {% endif %}
    <ul role="list" data-reveal-group>
      {% for recommendation in recommendations.products limit: 5 %}
        {% assign recommendation_variant = recommendation.selected_or_first_available_variant %}
        <li class="product-recommendations__item" data-reveal-item style="--reveal-order: {{ forloop.index0 }};">
          <a class="product-recommendations__image" href="{{ recommendation.url | escape }}" aria-label="{{ recommendation.title | escape }}">
            {% if recommendation.featured_image %}
              {{ recommendation.featured_image | image_url: width: 240 | image_tag: loading: 'lazy', widths: '120, 180, 240', sizes: '96px', alt: recommendation.featured_image.alt | default: recommendation.title | escape }}
            {% endif %}
          </a>
          <div class="product-recommendations__details">
            <h3 class="product-title-text heading-h3 heading-text"><a href="{{ recommendation.url | escape }}">{{ recommendation.title | escape }}</a></h3>
            <div class="product-recommendations__price{% if recommendation_variant.compare_at_price > recommendation_variant.price %} product-recommendations__price--sale{% endif %}">
              <span>{% render 'money', amount: recommendation_variant.price %}</span>
              {% if recommendation_variant.compare_at_price > recommendation_variant.price %}<s>{% render 'money', amount: recommendation_variant.compare_at_price %}</s>{% endif %}
            </div>
          </div>
          <a class="product-recommendations__quick-add" href="{{ recommendation.url | escape }}" aria-label="{{ 'product_recommendations.view_product' | t: product: recommendation.title | escape }}">+</a>
        </li>
      {% endfor %}
    </ul>
  {% endif %}
</div>
{% schema %}
{
  "name": "Product recommendations",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Complete the set"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        {
          "value": "h0",
          "label": "t:typography_options.display_extra_large"
        },
        {
          "value": "h1",
          "label": "t:typography_options.display_large"
        },
        {
          "value": "h2",
          "label": "t:typography_options.display_medium"
        },
        {
          "value": "h3",
          "label": "t:typography_options.heading_extra_large"
        },
        {
          "value": "h4",
          "label": "t:typography_options.heading_large"
        },
        {
          "value": "h5",
          "label": "t:typography_options.heading_medium"
        },
        {
          "value": "h6",
          "label": "t:typography_options.heading_small"
        },
        {
          "value": "h7",
          "label": "t:typography_options.heading_extra_small"
        },
        {
          "value": "custom",
          "label": "t:typography_options.custom"
        }
      ],
      "default": "h3"
    },
    {
      "type": "select",
      "id": "html_tag",
      "label": "HTML Tag",
      "options": [
        {
          "value": "h1",
          "label": "H1"
        },
        {
          "value": "h2",
          "label": "H2"
        },
        {
          "value": "h3",
          "label": "H3"
        },
        {
          "value": "h4",
          "label": "H4"
        },
        {
          "value": "h5",
          "label": "H5"
        },
        {
          "value": "h6",
          "label": "H6"
        }
      ],
      "default": "h2"
    }
  ],
  "presets": [
    {
      "name": "Product recommendations",
      "category": "Product discovery"
    }
  ]
}
{% endschema %}

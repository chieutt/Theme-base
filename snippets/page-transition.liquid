{% doc %}
  Renders the global page-loading overlay.
{% enddoc %}

<div class="page-transition" data-page-transition data-page-transition-duration="{{ settings.page_transition_duration | default: 1000 }}" role="status" aria-live="polite">
  <span class="visually-hidden">{{ 'accessibility.loading' | t }}</span>
</div>
